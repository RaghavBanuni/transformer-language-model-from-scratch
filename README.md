# transformer-language-model-from-scratch
A decoder-only transformer language model in pure NumPy: hand-derived backward passes verified against finite differences, byte-level BPE, AdamW, KV-cache generation
